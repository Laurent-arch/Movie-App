<%- include('./head.ejs') %>
<link rel="stylesheet" type="text/css"href="/style.css">
  </head>

   <nav class="nav">
     
     
      
      <ul class="link-container">
         <%  if (!isAuthenticated) { %>
        <li class="link" >
            <a href="/movies/login" class="a-link">Sign in</a>
        </li>
        <li class="link">
            <a href="/movies/signup" class="a-link">Signup</a>
        </li>
        <% } else { %>
       
          <form action="/movies/logout" method="post" class="form-navigation">
            <li class="link">
            <a href="/movies/insert" class="a-link-add">Add a new movie</a>
        </li>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button type="submit" class="btn-logout">log out</button>  
          </form>
        
           <% } %>
       
      </ul>
    </nav>