<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/login-form.css" />
    <title>Document</title>
  </head>
  <body>
    <main class="form-container">
      <% if(errorMessage) { %>
      <div class="error-message"><%= errorMessage %></div>
      <% } %>
      <form class="login-form" action="/movies/reset" method="POST">
        <div class="form-control">
          <label for="email" class="label">Enter your email</label><br />
          <input type="email" name="email" id="email" />
        </div>

        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div class="btn-container">
          
          <% if (!isAuthenticated) { %>
          <a href="/movies/signup"
            ><button class="btn-signup-reset" type="submit">Reset password</button></a
          >
          <% } %>
        </div>
      </form>
    </main>
  </body>
</html>
